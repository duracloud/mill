aws.accessKeyId={accessKeyId}
aws.secretKey={secretKey}
taskQueues={highPriority,lowerPriority,lowestPriority}
deadLetterQueue={deadLetterQueue}
credentialsFilePath=../common/src/test/resources/test.credentials.json
